<!DOCTYPE html>
<html lang="en">
    <!--HEAD--> 
    <%- include("../partials/head") %>

    <body>
        <!--HEADER-->
        <%- include("../partials/header") %>
      
        <main class="p-1 vw-90 mx-auto">
            <section class="section">
                <h1 class="title w-100 text-center p-1">PANEL ADMINISTRADOR</h1>
                
                <div class="flex flex-center">
                    <div class="p-1">
                        <button class="btn-primary" onclick="window.location.href='/productos/new';">Nuevo</button>
                    </div>
                    <div class="p-1">
                        <button class="btn-primary" onclick="window.location.href='/logout';">Cerrar Sesión</button>
                    </div>
                </div>

                <ul class="w-50 align-center"><br>
                    <% for( let i = 0; i < productos.length; i++ ) { %>
                        <li class="flex p-1">
                            <div class="w-75 bt-1 p-1"><%= productos[i].nombre %></div><div class="bt-1 p-1"><a class="btn-primary-admin" href="/productos/edit/<%= productos[i].id %>"> editar </a></div><div class="bt-1 p-1"><a class="btn-primary-admin" href="/productos/delete/<%= productos[i].id %>"> eliminar </a></div>
                        </li>
                    <% } %>
                </ul>
            </section>
        </main>

        <!--FOOTER-->
        <%- include("../partials/footer") %>
        
        <!--SCRIPTS-->
        <script src="/js/carrousel.js"></script>
    </body>
</html>